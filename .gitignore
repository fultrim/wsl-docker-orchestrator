# Logs & reports
reports/
*.log

# State (optional commit if you want history) - adjust if you prefer to track
state/context_state.json
state/history/

# Baseline images & snapshots are stored on K: outside the repo root
# (cannot be excluded here because they are not under version control path).

# Rootfs downloads (if placed under repo)
*.rootfs.tar.gz
*.tar
*.tar.gz

# PowerShell artifacts
*.ps1.tmp

# Misc
*.DS_Store
Thumbs.db
